using System;
using UnityEngine;

//Intentionally empty class. Used by Unity to reference the property drawer defined in ReadOnlyDrawer.cs(must be a seperate file)
public class ReadOnlyAttribute : PropertyAttribute { }
